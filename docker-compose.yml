services:
  xiaomusic-core-production:
    profiles: ["production"]
    image: akari787/xiaomusic-core:latest
    container_name: xiaomusic-core
    restart: unless-stopped
    ports:
      - "58090:8090"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
      XIAOMUSIC_PUBLIC_PORT: 58090
      XIAOMUSIC_DISABLE_HTTPAUTH: "false"
      XIAOMUSIC_HTTPAUTH_USERNAME: ${XIAOMUSIC_HTTPAUTH_USERNAME:-admin}
      API_SECRET: ${API_SECRET:-}
      HTTP_AUTH_HASH: ${HTTP_AUTH_HASH:-}
      HTTP_AUTH_PASSWORD: ${HTTP_AUTH_PASSWORD:-}
    volumes:
      - ./conf:/app/conf
      - ./music:/app/music
      - ./xiaomusic/webui/static:/app/xiaomusic/webui/static:ro

  xiaomusic-core-test:
    profiles: ["test"]
    image: akari787/xiaomusic-core:latest
    container_name: xiaomusic-core-test
    restart: unless-stopped
    ports:
      - "58091:8090"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
      XIAOMUSIC_PUBLIC_PORT: 58091
      XIAOMUSIC_DISABLE_HTTPAUTH: "false"
      XIAOMUSIC_HTTPAUTH_USERNAME: ${XIAOMUSIC_HTTPAUTH_USERNAME:-admin}
      API_SECRET: ${API_SECRET:-}
      HTTP_AUTH_HASH: ${HTTP_AUTH_HASH:-}
      HTTP_AUTH_PASSWORD: ${HTTP_AUTH_PASSWORD:-}
    volumes:
      - ./conf:/app/conf
      - ./music:/app/music
      - ./xiaomusic/webui/static:/app/xiaomusic/webui/static:ro
