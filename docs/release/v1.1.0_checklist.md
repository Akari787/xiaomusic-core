# v1.1.0 发布前 Checklist

适用版本：v1.1.0  
目标：按相对 `v1.0.10` 的真实代码变化做最终核对，不把 roadmap 计划项直接写成已发布事实。

---

## 1. 版本号检查

- [ ] `xiaomusic/__init__.py` 为 `1.1.0`
- [ ] `pyproject.toml` 为 `1.1.0`
- [ ] `/getversion`、CLI banner、日志版本来源仍统一指向 `__version__`
- [ ] `/api/v1/system/status` / OpenAPI / 日志中版本展示来源未出现冲突

---

## 2. Release Notes / Changelog 检查

- [ ] `CHANGELOG.md` 已补充 `v1.1.0`
- [ ] `docs/release/v1.1.0.md` 已存在且口径一致
- [ ] 发布说明明确写出“本次对比基线为 `v1.0.10`”
- [ ] 发布说明没有把未完成的 M2 P2、前端旧 vitest 漂移、浏览器自动化不稳定写成已解决

---

## 3. 文档入口检查

- [ ] `README.md` 已加入 `v1.1.0` 发布说明入口
- [ ] `docs/index.md` 已加入 `v1.1.0` 发布说明入口
- [ ] auth 主线阅读路径已更新到 `v1.1.0` 发布说明
- [ ] `docs/architecture/auth_runtime_recovery.md` 可从导航找到
- [ ] diagnostics 字段说明文档可从实现文档目录找到

---

## 4. Auth 核对

- [ ] `GET /api/v1/auth/status` 存在
- [ ] `system` / `v1` / debug 共用统一公开状态映射来源
- [ ] short-session 缺失后可进入 persistent-auth rebuild 主路径
- [ ] `MiJia` fallback 路径与阶段化 debug flow 结构仍在代码中

---

## 5. Diagnostics 核对

- [ ] `GET /diagnostics` 返回统一五维视图
- [ ] 顶层字段包含 `generated_at_ms` / `overall_status` / `summary` / `areas`
- [ ] 状态枚举固定为 `ok | degraded | failed | unknown`
- [ ] WebUI 已存在最小 diagnostics 页面入口

---

## 6. Sources 核对

- [ ] `SourcePluginManager` 已存在
- [ ] `/api/v1/sources` 与 `/api/v1/sources/reload` 可用
- [ ] `upload / uninstall / enable / disable` 路由已存在
- [ ] 发布说明已如实写明 `disabled_plugins` 当前仅为内存态
- [ ] WebUI 已存在最小 sources 管理页

---

## 7. Playback / Player State 核对

- [ ] `/api/v1/player/state` 与 `/api/v1/player/stream` 仍共用 snapshot 来源
- [ ] Jellyfin playlist context 下 `track.source` / `context` / `current_index` 链路已在当前代码中存在
- [ ] external URL post-start hook 仍负责 duration / timer / volume 的本地运行态建立
- [ ] `/api/v1/player/state` 返回中包含 `volume`
- [ ] `/api/v1/play` 与 `/api/v1/control/next` 路由层额外 `push_player_state_event()` 已收口，避免重复 SSE 事件

---

## 8. 范围核对

### 本次已确认范围

- [ ] auth 正式状态接口与 rebuild 主路径已落地
- [ ] startup/self-check 统一诊断视图已落地
- [ ] sources manager + API + 最小 WebUI 管理页已落地
- [ ] Jellyfin / playlist / player state 关键主链已按当前代码事实推进

### 本次未完成或未全量确认范围

- [ ] M2 P2 未完成
- [ ] `disabled_plugins` 不持久化
- [ ] 浏览器自动化不稳定仍存在
- [ ] 旧 HomePage vitest 漂移仍存在
- [ ] 不能把当前结果表述为“所有播放路径长期稳定性总验收通过”

---

## 9. tag / release 前最终核对项

- [ ] 工作区无非发布残留文件
- [ ] 版本号、CHANGELOG、release notes、checklist 已提交
- [ ] README / docs 首页 / CHANGELOG / release notes 口径一致
- [ ] tag 名称使用 `v1.1.0`
- [ ] 发布正文不出现“全部完成”“全量稳定”“所有边界均已覆盖”等超范围表述
