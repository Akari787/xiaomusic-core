# v1.1.3 发布检查单

发布日期：2026-07-23
基线：`v1.1.2`

## 1. 发布范围

- [ ] `pyproject.toml` 与 `xiaomusic/__init__.py` 均为 `1.1.3`
- [ ] 发布提交只包含本轮播放、WebUI、测试、文档和版本文件
- [ ] 不包含 `xiaomusic/auth.py`、`tests/test_auth_recovery_cooldown.py`、`CHANGELOG.md`
- [ ] 不包含 Songloft 文档、临时提交信息或生成的架构 HTML

## 2. 代码门禁

- [ ] Python 全量测试通过
- [ ] 修改文件的 Ruff 检查通过
- [ ] WebUI 测试通过
- [ ] WebUI production build 通过
- [ ] `git diff --check` 通过

## 3. 播放控制门禁

- [ ] `get_offset_duration()` 保持纯读
- [ ] timer 最多延期三次，第四次推进下一首
- [ ] 后台启动确认不破坏性重播同曲
- [ ] 当前后台任务不会被 session bump 自取消
- [ ] 新随机 session 只洗牌一次
- [ ] next/previous 不调用 `/api/v1/play`
- [ ] Facade 不包含 session-aware 队列旁路
- [ ] next/previous 最终统一进入 `XiaoMusicDevice._play_next/_play_prev`

## 4. WebUI 与安全门禁

- [ ] `/webui/` 返回 200
- [ ] `index.html` 引用的实际 JS/CSS 均返回 200 且非空
- [ ] Public API 响应不包含明文 Jellyfin API key
- [ ] 构建与发布日志不输出凭据

## 5. 实机验收

- [ ] 随机歌单成功建立，context 保持目标歌单
- [ ] 轨迹满足 `N → N+1 → N+2 → N+1`
- [ ] 整段只有一次 external playback session 与一次 shuffle
- [ ] 每次切歌后 `play_session_id` 变化且 `transport_state=playing`
- [ ] 不出现 `retry_same_song_with_sync_stop`
- [ ] 不出现 `duration=0` 半启动卡死或 epoch 级 offset

## 6. Git 与 GitHub

- [ ] 提交信息符合 Conventional Commits
- [ ] 提交已推送到 `origin/main`
- [ ] 创建 annotated tag `v1.1.3` 并推送
- [ ] GitHub Release 使用 `docs/release/v1.1.3.md`
- [ ] 触发 `release-image-assets.yml`，tag 输入为 `v1.1.3`
- [ ] amd64、arm64、arm-v7 三个附件构建成功
- [ ] Release 页面、tag 与 `origin/main` 指向同一提交
