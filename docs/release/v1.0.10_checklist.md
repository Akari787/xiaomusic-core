# v1.0.10 发布前 Checklist

适用版本：v1.0.10  
目标：发布前最终核对，不扩写当前已通过范围。

---

## 1. 版本号检查

- [ ] `xiaomusic/__init__.py` 为 `1.0.10`
- [ ] `pyproject.toml` 为 `1.0.10`
- [ ] `/getversion` 对外版本来源仍指向 `__version__`
- [ ] `/api/v1/system/status` / OpenAPI / 日志中版本展示来源未出现冲突

---

## 2. Release Notes 检查

- [ ] `CHANGELOG.md` 已补充 `v1.0.10`
- [ ] `docs/release/v1.0.10.md` 已存在且口径一致
- [ ] 发布说明明确写出“本次确认的是哪一层，不是哪一层”
- [ ] 未把 `_try_login()` 主路径通过扩写成 spec rebuild 全量通过

---

## 3. 文档入口检查

- [ ] README 文档导航包含当前 auth 主线阅读路径
- [ ] `docs/index.md` 包含当前 auth 主线阅读路径
- [ ] `auth_runtime_recovery.md` 与 `auth_runtime_reload_recovery_path.md` 导航顺序正确
- [ ] `auth_fresh_session_runtime_recovery_acceptance.md` 可作为当前验收收口入口
- [ ] spec rebuild 验收矩阵已入仓并可从导航找到

---

## 4. auth status / debug status 检查

- [ ] `/api/auth/status` 可用
- [ ] `/api/v1/debug/auth_state` 可用
- [ ] `/api/v1/debug/auth_runtime_reload_state` 可用
- [ ] `/api/v1/debug/miaccount_login_trace` 可用
- [ ] 本轮观察内 status/debug 业务结论一致

---

## 5. manual refresh_runtime smoke

- [ ] `POST /api/auth/refresh_runtime` smoke 通过
- [ ] `POST /api/auth/refresh` smoke 通过
- [ ] 手动触发后关键字段可见：
  - [ ] `login_result`
  - [ ] `candidate_runtime_account_ready`
  - [ ] `candidate_runtime_cookie_ready`
  - [ ] `verify_attempted`
  - [ ] `runtime_swap_applied`

---

## 6. playback smoke

- [ ] `local_library` 首轮起播 smoke 通过
- [ ] `direct_url` 首轮起播 smoke 通过
- [ ] `GET /api/v1/player/state` 基本契约可读

---

## 7. 范围核对

### 本次已通过范围

- [ ] fresh session 修补后的 `_try_login()` 主路径稳定
- [ ] status/debug 在本轮观察内业务结论一致
- [ ] `local_library` / `direct_url` 首轮 start confirmation 有局部实机证据

### 本次未覆盖范围

- [ ] spec rebuild 全量
- [ ] auto runtime reload 全量
- [ ] singleflight / fallback 全量
- [ ] auth / playback 交叉边界
- [ ] 极端网络扰动

---

## 8. tag / release 前最终核对项

- [ ] 工作区无非发布文件残留
- [ ] 版本号、CHANGELOG、release notes、checklist 已提交
- [ ] 发布说明与 README / docs 首页口径一致
- [ ] tag 名称使用 `v1.0.10`
- [ ] 发布正文不出现“spec rebuild 全量通过”等超范围表述
