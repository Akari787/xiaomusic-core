# v1.1.7 发布检查清单

日期：2026-09-22
分支：`fix/auth-refresh-state-machine`
发布源：tag `v1.1.7` 指向的提交；已验证运行时代码提交 `58a4361d`（其后仅发布文档变更）
源 artifact SHA256（基于 `58a4361d`）：`27789efda2e8a34eba28a929600ef3f0c14dd1cc77793cacd754679c1fd8fdce`

## 1. 版本与文件

- [x] `xiaomusic/__init__.py` 与 `pyproject.toml` 为 `1.1.7`
- [x] `CHANGELOG.md` 顶部为 `v1.1.7 (2026-09-22)`，语义与最终代码一致
- [x] `docs/release/v1.1.7.md` 已按最终实现与现场数据重写
- [x] 文档不含 token、密码、bcrypt 实值或 API key
- [x] `.env.example` 的凭据示例使用单引号，避免 Compose `$` 插值

## 2. 认证状态机

- [x] 无账号密码时不执行 full login；候选使用隔离 `MemoryTokenStore`
- [x] TTL 参与调度，unknown TTL 以 12h scheduled 辅助轮转
- [x] `70016` → `credential_session_rejected`；`87001`/captcha → `interactive_captcha_challenge`
- [x] scheduled 命中上述拒绝时保留 healthy 并挂起后续刷新
- [x] DNS/connector/errno 沿异常链分类；健康探测网络瞬断只进 cooldown
- [x] cooldown 后先重探现有 runtime，恢复时不登录、不清除 scheduled 挂起
- [x] `auth_call` 在 DNS 网络故障下不调度后台恢复
- [x] `is_network_error` 只把 HTTP 500–599 视为网络状态，不吞业务码
- [x] QR 后 verified atomic rebind，失败保留旧 runtime

## 3. 内网部署与安全边界

- [x] 默认 `XIAOMUSIC_DISABLE_HTTPAUTH=true`，无凭据可启动
- [x] 显式 `false` 时 CLI 才 fail-closed 要求凭据
- [x] `Config` 与 CLI 对开关值统一 `strip().lower()` 归一
- [x] `docker-compose.hardened.yml` 为 opt-in 鉴权模板
- [x] 插件/OpenAPI/插件源路由使用可覆盖的 `verification`，noauth 下匿名可用
- [x] 上传拒绝空名、裸 `.js`、NUL、绝对路径、正反斜杠子目录、目录穿越
- [x] 上传文件名预检只扫描 multipart part header，不扫描文件内容
- [x] `AuthStaticFiles` 不使用 `assert` 执行认证

## 4. 媒体数据面

- [x] `/music/*`、`/picture/*` 匿名可达
- [x] `/static/silence.mp3`、`/static/search.mp3` 匿名可达
- [x] 显式启用 Basic 时其它 static 与管理写接口受保护；默认 noauth 模式按内网部署选择开放
- [x] 匿名 `/proxy` 只接受服务端签发的 `t.<token>`
- [x] legacy base64 proxy 在启用 Basic 时要求有效凭据
- [x] `Path.resolve()` + `relative_to()` 阻断目录回溯、兄弟前缀与符号链接越界

## 5. 依赖与构建

- [x] axios `1.20.0`、follow-redirects `1.16.0`、form-data `4.0.6`、qs `6.16.0`、undici `7.29.1`
- [x] `npm audit --registry=https://registry.npmjs.org --omit=dev`：0
- [x] Node 安全测试 10/10
- [x] ARMv7 Dockerfile 使用 Alpine 并补齐 `cargo`、`linux-headers`
- [x] 多架构镜像交由 tag CI 构建（本机不手工构建）

## 6. 本地门禁

- [x] 全量 pytest：`1191 passed / 106 failed / 2 skipped / 1 warning`，未新增失败
- [x] Ruff（变更文件）、`compileall`、`git diff --check` 通过
- [x] 源 artifact SHA256 已记录

## 7. 测试服务器验收

- [x] project `xiaomusic_core_smoke`，端口 `58090`
- [x] 候选镜像 `xiaomusic-core:v1.1.7-final-rc`，image ID `sha256:a68950d22fcdc258c2cfeed8e94ce46d3d80b62f2685e6436c4f04b0e317613d`
- [x] `StartedAt=2026-09-22T01:20:22Z`，`restart_count=0`
- [x] 隔离无凭据启动：`/getversion=200`、`/api/js-plugins=200`
- [x] `/getversion=1.1.7`；`/` 302→200
- [x] 插件、admin legacy、媒体、占位音频匿名 200
- [x] invalid proxy token 400；兄弟前缀越界 404
- [x] 上传穿越 400 且未落盘；正文含 `filename="a/b"` 的合法上传 200
- [x] 连续运行超过 3 小时 30 分钟：`auth_state=healthy`，recovery/probe/retry/lock 计数均为 0，health/keepalive probe 为 `ok`
- [x] scheduled refresh 仅出现 1 次预期 `70016`，随后 `scheduled_refresh_suspended=true`；无重复登录刺激、认证失败、87001、keepalive 不健康或 Traceback
- [x] 临时文件清理完成；`.env`、`setting.json` 权限 600
- [x] 回滚镜像与救援备份已记录；部署脚本保留失败自动回滚

## 8. 发布流程

- [x] push 候选分支
- [x] 无斜杠验证分支 AMD64 CI 构建、Python import、Node 安全测试通过
- [ ] fast-forward `main` 并 push
- [ ] 创建并 push annotated tag `v1.1.7`
- [ ] tag CI 三架构构建通过，并完成 Docker Hub 多架构推送与 GitHub Release
