# v1.1.7 发布检查清单

日期：2026-09-21

- 正式基线：`v1.1.6`（`bf76e739`）
- 功能 HEAD：`bbc3bcd8`
- 版本准备 commit：`acfb0718`
- 文档语义修正 commit：`933e50ab`
- 部署 artifact 源 HEAD：`5660a067`（该 HEAD 生成九个 runtime artifact 文件）
- 初始现场部署记录 commit：`1935640b`
- 分支：`fix/auth-refresh-state-machine`

## 1. 版本与文件

- [x] `xiaomusic/__init__.py` 为 `1.1.7`
- [x] `pyproject.toml` 为 `1.1.7`
- [x] `CHANGELOG.md` 顶部为 `v1.1.7 (2026-09-21)`，v1.1.6 历史正文未改
- [x] `docs/release/v1.1.7.md` 已归档发布说明
- [x] 本文件已归档检查结果
- [x] 文档不包含 token、凭据或 API key

## 2. 本地代码门禁

- [x] startup 模块独立运行：
  `python -m pytest tests/unit/test_auth_startup_ttl_sync.py` → **13 passed**
- [x] 最终关键组合运行：
  `python -m pytest tests/unit/test_auth_startup_ttl_sync.py tests/test_qrcode_auth_rebind.py tests/unit/test_auth_classification_regression.py tests/test_auth_runtime_stability.py tests/unit/test_auth_short_session_rebuild.py tests/test_auth_refresh_api.py tests/test_getalldevices_recovery.py` → **118 passed**，1 个 `RequestsDependencyWarning`
- [ ] `tests/test_system_diagnostics.py` 无新增回归；基线独立 worktree 已知 2 failures：fixture 缺 `map_auth_public_status`
- [ ] `tests/unit/test_auth_self_heal.py` 不作为本版回归证据：旧 dead-path/API 漂移，未经本轮实测不记录精确失败数
- [x] 本分支新增 lint 修复：仅排序 `tests/test_qrcode_auth_rebind.py` import；指定 Ruff 组合通过
- [x] `python -m compileall -q xiaomusic tests` 通过
- [x] `git diff --check` 通过
- [ ] 本机包构建未完成：PDM 在 Python 3.14 环境因 argparse 兼容异常不可执行，且 `build` 模块缺失
- [x] Docker 完整构建通过：image digest `sha256:5a64eb74f0600e464484e7b7c157b01f02c12f853b3432e92ab9fffb27ccfa57`
- [x] 构建边界已记录：仓库无 `pdm.lock`，Docker 缺锁时走 `pdm install`
- [x] 构建日志 npm audit：5 项（2 moderate、3 high），本认证版本未顺手修复，留作待处理边界

## 3. 现场验收记录

- [x] `auth-sm-29de19ae`：旧 passToken 70016；1 次 scheduled 请求；分类 `credential_session_rejected`；旧 runtime healthy + suspended；`failure_count=0`；约 2.5h 零重复网络；`MiAccount.login=0`
- [x] 分类语义：`70016 + captchaUrl=null` 为 `credential_session_rejected`；`87001`/truthy captcha 为 `interactive_captcha_challenge`；两者均为 `long_term_expired=false`，不代表长期过期
- [x] 用户 QR 新凭据：`29de19ae` 临时 `0.1h/1m/.3`；saveTime `14:53:18.682`、`15:03:22.396` JST；间隔 `10m04s`；两次 exchange/service_token_written/verify/rebind ok；70016、87001、`MiAccount.login`、`login returned false` 均 0；已恢复 `12h/30m/.3`
- [x] 播放实机：明确授权后 volume 请求 0、物理回读最低 4；10 秒纯静音 direct_url；返回 `playing/direct_url/mina`；raw status 1；权威 state `playing`；stop 后 status 2、10 秒无复播；临时文件删除；再次 stop/probe status 2
- [ ] QR post-save verified rebind：自动测试覆盖磁盘新 auth → candidate → verify → swap 与零 exchange/login；未再次要求用户扫码做第二遍真实 handler 端到端实机验收
- [x] startup `login_at` 现场确认：`saveTime=1789984929363` → `login_at=1789984929.363`；容器 StartedAt 为 JST 19:07:15，首条 auth 启动日志为 JST 19:07:18，fast rebind 于 JST 19:07:49 收敛，即距首条 auth 启动日志约 31 秒、距容器 StartedAt 约 34 秒；随后 healthy/ready，计数为 70016=0、87001=0、`MiAccount.login=0`、`login returned false=0`、Traceback=0、ERROR=0，fast rebind ok=1

## 4. 发布动作边界

- [x] 本地 release commit 已准备
- [x] 测试服务器已部署：固定 Compose project `xiaomusic_core_smoke`，`xiaomusic-core:v1.1.7`
- [x] runtime artifact：九文件，SHA256 `38242060b5d732625e56f63df7b3b3093082b02a1bde58b5a26caeccde192df8`
- [x] `/getversion=1.1.7`；StartedAt `2026-09-21T10:07:15.430252639Z`（JST 19:07:15）；JST 19:10 restart=0、project 正确、根路径 HTTP 302
- [x] Docker build 输出 manifest-list digest，且 `docker image inspect ... --format {{.Id}}` 回读同值：local image ID / manifest-list digest `sha256:5a64eb74f0600e464484e7b7c157b01f02c12f853b3432e92ab9fffb27ccfa57`
- [x] 禁止触碰正式服务器；本次正式服务器未触碰
- [x] 不推送
- [x] 不打 tag
- [x] 不创建 GitHub Release
- [x] 提交前确认工作树只包含版本、CHANGELOG、两份 release 文档与本分支 lint 修复

## 5. 回滚坐标

- 备份：`/root/xiaomusic_core_candidates/v1.1.5-final/backups/v1.1.7-before-deploy-20260921-190618.tar.gz`
- 回滚镜像：`xiaomusic-core:rollback-v1.1.7-20260921-190618`
- Compose：`/root/xiaomusic_core_candidates/v1.1.5-final/docker-compose.v1.1.7.yml`
