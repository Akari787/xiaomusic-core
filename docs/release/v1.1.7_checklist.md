# v1.1.7 发布检查清单

日期：2026-09-21  
基线：`bbc3bcd8`  
分支：`fix/auth-refresh-state-machine`

## 1. 版本与文件

- [x] `xiaomusic/__init__.py` 为 `1.1.7`
- [x] `pyproject.toml` 为 `1.1.7`
- [x] `CHANGELOG.md` 顶部为 `v1.1.7 (2026-09-21)`，v1.1.6 历史正文未改
- [x] `docs/release/v1.1.7.md` 已归档发布说明
- [x] 本文件已归档检查结果
- [x] 文档不包含 token、凭据或 API key

## 2. 本地代码门禁

- [x] startup 模块独立运行：
  `python -m pytest tests/unit/test_auth_startup_ttl_sync.py` → **13 passed**
- [x] 最终关键组合运行：
  `python -m pytest tests/unit/test_auth_startup_ttl_sync.py tests/test_qrcode_auth_rebind.py tests/unit/test_auth_classification_regression.py tests/test_auth_runtime_stability.py tests/unit/test_auth_short_session_rebuild.py tests/test_auth_refresh_api.py tests/test_getalldevices_recovery.py` → **118 passed**，1 个 `RequestsDependencyWarning`
- [ ] `tests/test_system_diagnostics.py` 无新增回归；基线独立 worktree 已知 2 failures：fixture 缺 `map_auth_public_status`
- [ ] `tests/unit/test_auth_self_heal.py` 不作为本版回归证据：旧 dead-path/API 漂移，未经本轮实测不记录精确失败数
- [x] ruff 仅检查本轮改动 Python 文件并通过（`python -m ruff check xiaomusic/__init__.py`）
- [x] `python -m compileall -q xiaomusic tests` 通过
- [x] `git diff --check` 通过
- [x] 构建模块不可用（`No module named build`）；源码 import 验证通过：`xiaomusic.__version__ == 1.1.7`

## 3. 现场验收记录

- [x] `auth-sm-29de19ae`：旧 passToken 70016；1 次 scheduled 请求；分类 `credential_session_rejected`；旧 runtime healthy + suspended；`failure_count=0`；约 2.5h 零重复网络；`MiAccount.login=0`
- [x] 用户 QR 新凭据：`29de19ae` 临时 `0.1h/1m/.3`；saveTime `14:53:18.682`、`15:03:22.396` JST；间隔 `10m04s`；两次 exchange/service_token_written/verify/rebind ok；70016、87001、`MiAccount.login`、`login returned false` 均 0；已恢复 `12h/30m/.3`
- [x] 播放实机：明确授权后 volume 请求 0、物理回读最低 4；10 秒纯静音 direct_url；返回 `playing/direct_url/mina`；raw status 1；权威 state `playing`；stop 后 status 2、10 秒无复播；临时文件删除；再次 stop/probe status 2
- [ ] QR post-save verified rebind：自动测试覆盖磁盘新 auth → candidate → verify → swap 与零 exchange/login；未再次要求用户扫码做第二遍真实 handler 端到端实机验收
- [ ] startup `login_at`：自动测试覆盖；待 v1.1.7 重部署现场启动确认

## 4. 发布动作边界

- [x] 本轮仅准备本地 release commit
- [x] 不部署
- [x] 不推送
- [x] 不打 tag
- [x] 不创建 GitHub Release
- [x] 提交前确认工作树只包含版本、CHANGELOG 与两份 release 文档
