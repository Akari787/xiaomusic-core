import{_ as a,c as t,a0 as o,o as d}from"./chunks/framework.B6JXc2q-.js";const u=JSON.parse('{"title":"播放器状态 SSE 推送协议规范（Player Stream SSE Spec）","description":"","frontmatter":{},"headers":[],"relativePath":"spec/player_stream_sse_spec.md","filePath":"spec/player_stream_sse_spec.md"}'),i={name:"spec/player_stream_sse_spec.md"};function s(r,e,c,l,n,h){return d(),t("div",null,e[0]||(e[0]=[o(`<h1 id="播放器状态-sse-推送协议规范-player-stream-sse-spec" tabindex="-1">播放器状态 SSE 推送协议规范（Player Stream SSE Spec） <a class="header-anchor" href="#播放器状态-sse-推送协议规范-player-stream-sse-spec" aria-label="Permalink to &quot;播放器状态 SSE 推送协议规范（Player Stream SSE Spec）&quot;">​</a></h1><p>版本：v1.0 状态：正式规范 最后更新：2026-03-22 适用范围：<code>GET /api/v1/player/stream</code>、WebUI SSE 消费路径、服务端 SSE 推送实现</p><hr><h2 id="_1-文档定位" tabindex="-1">1. 文档定位 <a class="header-anchor" href="#_1-文档定位" aria-label="Permalink to &quot;1. 文档定位&quot;">​</a></h2><p>本文档定义 <code>GET /api/v1/player/stream</code> 的 SSE（Server-Sent Events）推送协议，包括接口定义、连接行为、事件结构、推送时机、重连机制与前端消费约束。</p><p>本文档是<strong>传输协议规范</strong>，不重新定义播放状态字段的语义。SSE 事件中携带的状态数据，必须完全遵守 <code>docs/spec/player_state_projection_spec.md</code>（以下简称&quot;投影规范&quot;）所定义的权威播放状态快照模型。本文档不引入任何投影规范之外的字段语义。</p><h3 id="_1-1-sse-是播放状态的主通道" tabindex="-1">1.1 SSE 是播放状态的主通道 <a class="header-anchor" href="#_1-1-sse-是播放状态的主通道" aria-label="Permalink to &quot;1.1 SSE 是播放状态的主通道&quot;">​</a></h3><p><code>GET /api/v1/player/stream</code> 是前端获取播放器状态的<strong>主通道</strong>。在 SSE 连接正常建立且运行的情况下，前端必须通过 SSE 接收播放状态更新，而不是通过轮询。</p><p><code>GET /api/v1/player/state</code> 的定位为<strong>初始化与降级回退通道</strong>，适用于以下场景：</p><ul><li>页面首次加载、SSE 连接尚未建立时，用于获取初始状态快照。</li><li>SSE 连接断开且重连尚未成功时，用于轮询兜底。</li><li>调试与状态核验目的。</li></ul><p>任何情况下，前端都不得将 SSE 与 <code>/player/state</code> 轮询并行运行并让两者竞争写入 UI。在 SSE 连接正常运行期间，轮询必须停止。</p><h3 id="_1-2-废弃说明" tabindex="-1">1.2 废弃说明 <a class="header-anchor" href="#_1-2-废弃说明" aria-label="Permalink to &quot;1.2 废弃说明&quot;">​</a></h3><p>本文档所定义的 SSE 机制取代了历史上曾讨论或试用过的 WebSocket 状态推送方案。WebSocket 方案不属于本规范的范围，不得在新实现中引入。</p><hr><h2 id="_2-接口定义" tabindex="-1">2. 接口定义 <a class="header-anchor" href="#_2-接口定义" aria-label="Permalink to &quot;2. 接口定义&quot;">​</a></h2><h3 id="_2-1-路径与方法" tabindex="-1">2.1 路径与方法 <a class="header-anchor" href="#_2-1-路径与方法" aria-label="Permalink to &quot;2.1 路径与方法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /api/v1/player/stream</span></span></code></pre></div><p>本接口属于 v1 Public API，遵守 <code>docs/api/api_v1_spec.md</code> 的命名空间与分级规则。本接口归属 Class C（查询型），不要求 <code>transport</code>，不触发任何设备侧动作。</p><h3 id="_2-2-查询参数" tabindex="-1">2.2 查询参数 <a class="header-anchor" href="#_2-2-查询参数" aria-label="Permalink to &quot;2.2 查询参数&quot;">​</a></h3><table tabindex="0"><thead><tr><th>参数</th><th>类型</th><th>必须</th><th>说明</th></tr></thead><tbody><tr><td><code>device_id</code></td><td><code>string</code></td><td>是</td><td>订阅的目标设备 ID，非空字符串</td></tr></tbody></table><p><code>device_id</code> 必须是已注册设备的合法 ID。一个 SSE 连接只订阅一个设备，不允许省略 <code>device_id</code> 或通过通配符订阅多个设备。</p><h3 id="_2-3-请求-header" tabindex="-1">2.3 请求 Header <a class="header-anchor" href="#_2-3-请求-header" aria-label="Permalink to &quot;2.3 请求 Header&quot;">​</a></h3><p>客户端发起请求时，应携带以下 Header：</p><table tabindex="0"><thead><tr><th>Header</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>Accept</code></td><td><code>text/event-stream</code></td><td>表明客户端期望接收 SSE 流</td></tr><tr><td><code>Cache-Control</code></td><td><code>no-cache</code></td><td>禁止中间缓存干扰 SSE 流</td></tr></tbody></table><h3 id="_2-4-响应-header" tabindex="-1">2.4 响应 Header <a class="header-anchor" href="#_2-4-响应-header" aria-label="Permalink to &quot;2.4 响应 Header&quot;">​</a></h3><p>服务端响应必须包含以下 Header：</p><table tabindex="0"><thead><tr><th>Header</th><th>值</th></tr></thead><tbody><tr><td><code>Content-Type</code></td><td><code>text/event-stream; charset=utf-8</code></td></tr><tr><td><code>Cache-Control</code></td><td><code>no-cache</code></td></tr><tr><td><code>Connection</code></td><td><code>keep-alive</code></td></tr><tr><td><code>X-Accel-Buffering</code></td><td><code>no</code></td></tr></tbody></table><p><code>X-Accel-Buffering: no</code> 必须存在，用于禁止 Nginx 等反向代理对 SSE 流进行缓冲，确保事件能实时到达客户端。</p><h3 id="_2-5-http-状态码" tabindex="-1">2.5 HTTP 状态码 <a class="header-anchor" href="#_2-5-http-状态码" aria-label="Permalink to &quot;2.5 HTTP 状态码&quot;">​</a></h3><table tabindex="0"><thead><tr><th>状态码</th><th>语义</th></tr></thead><tbody><tr><td><code>200 OK</code></td><td>连接建立成功，SSE 流开始</td></tr><tr><td><code>400 Bad Request</code></td><td>缺少 <code>device_id</code> 或参数非法</td></tr><tr><td><code>401 Unauthorized</code></td><td>鉴权失败</td></tr><tr><td><code>404 Not Found</code></td><td>指定 <code>device_id</code> 对应的设备不存在</td></tr><tr><td><code>503 Service Unavailable</code></td><td>服务端当前无法提供 SSE 服务</td></tr></tbody></table><p><code>400</code>、<code>401</code>、<code>404</code>、<code>503</code> 响应必须遵守 <code>api_v1_spec.md</code> 的统一错误模型，以 JSON 格式返回结构化错误（<code>code</code>、<code>message</code>、<code>data.error_code</code>、<code>data.stage</code>、<code>request_id</code>），而不是纯文本错误消息。</p><hr><h2 id="_3-连接行为" tabindex="-1">3. 连接行为 <a class="header-anchor" href="#_3-连接行为" aria-label="Permalink to &quot;3. 连接行为&quot;">​</a></h2><h3 id="_3-1-建立连接后的初始推送" tabindex="-1">3.1 建立连接后的初始推送 <a class="header-anchor" href="#_3-1-建立连接后的初始推送" aria-label="Permalink to &quot;3.1 建立连接后的初始推送&quot;">​</a></h3><p>服务端在 SSE 连接成功建立后，必须<strong>立即</strong>推送一条 <code>player_state</code> 事件，携带该设备当前最新的完整状态快照。</p><p>此初始推送是强制行为，不得省略。其目的是确保前端在切换到 SSE 模式时，能够立即得到一份权威状态，而不是等待下一次状态变化才收到第一条事件。</p><h3 id="_3-2-长连接语义" tabindex="-1">3.2 长连接语义 <a class="header-anchor" href="#_3-2-长连接语义" aria-label="Permalink to &quot;3.2 长连接语义&quot;">​</a></h3><p>本接口是长连接，连接建立后应持续保持，直至以下情况之一发生：</p><ul><li>客户端主动关闭连接（例如页面卸载、用户导航离开）。</li><li>服务端发生无法恢复的异常，需要主动关闭连接（见第 3.3 节）。</li></ul><p>在连接存续期间，服务端不得因为播放器状态长时间未变化而主动关闭连接。长时间无状态变化时，服务端必须依靠心跳机制（见第 8 节）维持连接活跃，而不是关闭连接。</p><h3 id="_3-3-服务端主动关闭的限制" tabindex="-1">3.3 服务端主动关闭的限制 <a class="header-anchor" href="#_3-3-服务端主动关闭的限制" aria-label="Permalink to &quot;3.3 服务端主动关闭的限制&quot;">​</a></h3><p>服务端仅在以下情况下允许主动关闭 SSE 连接：</p><ul><li>服务端进程即将退出或重启。</li><li>客户端的会话鉴权已过期且无法自动续期（服务端在关闭前应推送一条错误事件，见第 10.3 节）。</li><li>服务端内部出现无法恢复的错误，导致无法继续推送状态。</li></ul><p>服务端不得因以下原因主动关闭连接：</p><ul><li>播放器进入 <code>idle</code> 或 <code>stopped</code> 状态。</li><li>订阅的设备当前不在播放中。</li><li>长时间未产生新的播放事件（此时应使用心跳维持）。</li></ul><hr><h2 id="_4-事件结构" tabindex="-1">4. 事件结构 <a class="header-anchor" href="#_4-事件结构" aria-label="Permalink to &quot;4. 事件结构&quot;">​</a></h2><h3 id="_4-1-标准-sse-格式" tabindex="-1">4.1 标准 SSE 格式 <a class="header-anchor" href="#_4-1-标准-sse-格式" aria-label="Permalink to &quot;4.1 标准 SSE 格式&quot;">​</a></h3><p>所有播放状态事件必须使用以下标准 SSE 格式：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>event: player_state</span></span>
<span class="line"><span>id: &lt;event_id&gt;</span></span>
<span class="line"><span>data: &lt;JSON 序列化的状态快照&gt;</span></span></code></pre></div><p>每条事件以空行结束（即 <code>\\n\\n</code>），这是标准 SSE 协议要求。</p><h3 id="_4-2-字段说明" tabindex="-1">4.2 字段说明 <a class="header-anchor" href="#_4-2-字段说明" aria-label="Permalink to &quot;4.2 字段说明&quot;">​</a></h3><p><strong><code>event</code></strong>：事件类型，播放状态事件的类型固定为 <code>player_state</code>，不得使用其他值。</p><p><strong><code>id</code></strong>：事件 ID，必须存在。其值必须为该事件所携带状态快照的 <code>revision</code> 值（字符串形式）。此设计使浏览器在自动重连时能通过 <code>Last-Event-ID</code> 请求头上报最后收到的事件 ID，服务端可据此判断客户端状态（见第 7 节）。</p><p><strong><code>data</code></strong>：必须是合法 JSON，内容必须是完整的权威播放状态快照对象，完全符合投影规范第 5 节所定义的字段模型。不允许对快照字段做任何裁剪、重命名或结构变形。</p><h3 id="_4-3-data-字段的完整性要求" tabindex="-1">4.3 data 字段的完整性要求 <a class="header-anchor" href="#_4-3-data-字段的完整性要求" aria-label="Permalink to &quot;4.3 data 字段的完整性要求&quot;">​</a></h3><p><code>data</code> 字段中的状态快照必须包含投影规范中所有标记为&quot;必须存在&quot;的字段，具体包括：</p><ul><li><code>device_id</code></li><li><code>revision</code></li><li><code>play_session_id</code></li><li><code>transport_state</code></li><li><code>track</code>（对象或 <code>null</code>，含 <code>track.id</code>、<code>track.title</code>）</li><li><code>context</code>（对象或 <code>null</code>，含 <code>context.id</code>、<code>context.name</code>、<code>context.current_index</code>）</li><li><code>position_ms</code></li><li><code>duration_ms</code></li><li><code>snapshot_at_ms</code></li></ul><p>每次推送必须携带完整快照，不得仅推送变化的字段（即不使用增量/差异推送）。前端必须每次以完整快照替换本地状态，而不是将事件数据合并到旧状态上。</p><h3 id="_4-4-心跳事件格式" tabindex="-1">4.4 心跳事件格式 <a class="header-anchor" href="#_4-4-心跳事件格式" aria-label="Permalink to &quot;4.4 心跳事件格式&quot;">​</a></h3><p>心跳事件不是 <code>player_state</code> 事件，必须使用 SSE 注释格式，不携带 <code>event</code> 或 <code>data</code> 字段：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>: heartbeat</span></span></code></pre></div><p>心跳事件不携带状态数据，前端收到心跳后仅需重置连接超时计时器，不得将其作为状态更新处理。</p><h3 id="_4-5-错误事件格式" tabindex="-1">4.5 错误事件格式 <a class="header-anchor" href="#_4-5-错误事件格式" aria-label="Permalink to &quot;4.5 错误事件格式&quot;">​</a></h3><p>当服务端需要在关闭连接前通知客户端发生了错误（见第 10.3 节），应推送一条 <code>stream_error</code> 事件，然后关闭连接：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>event: stream_error</span></span>
<span class="line"><span>data: {&quot;error_code&quot;: &quot;&lt;ERROR_CODE&gt;&quot;, &quot;message&quot;: &quot;&lt;说明&gt;&quot;}</span></span></code></pre></div><p><code>stream_error</code> 事件中的 <code>error_code</code> 应使用 <code>api_v1_spec.md</code> 中定义的错误码体系，<code>message</code> 为可读说明。客户端收到 <code>stream_error</code> 后，应根据 <code>error_code</code> 决定是否发起重连，而不是无条件重连。</p><hr><h2 id="_5-推送时机" tabindex="-1">5. 推送时机 <a class="header-anchor" href="#_5-推送时机" aria-label="Permalink to &quot;5. 推送时机&quot;">​</a></h2><h3 id="_5-1-必须推送新事件的情况" tabindex="-1">5.1 必须推送新事件的情况 <a class="header-anchor" href="#_5-1-必须推送新事件的情况" aria-label="Permalink to &quot;5.1 必须推送新事件的情况&quot;">​</a></h3><p>以下任何情况发生时，服务端必须推送一条新的 <code>player_state</code> 事件。这些情况与投影规范第 6.2 节定义的 <code>revision</code> 递增触发条件完全对应：</p><ul><li><strong>曲目切换</strong>：当前播放曲目切换到另一首，<code>play_session_id</code> 发生变化。</li><li><strong>传输状态变化</strong>：<code>transport_state</code> 枚举值发生任何变化，包括但不限于 <code>playing → switching</code>、<code>switching → playing</code>、<code>playing → paused</code>、<code>paused → playing</code>、<code>playing → stopped</code>、任意状态 → <code>error</code>。</li><li><strong>播放会话变化</strong>：<code>play_session_id</code> 发生变化的任何场景。</li><li><strong>曲目身份变化</strong>：<code>track.id</code> 发生变化。</li><li><strong>上下文变化</strong>：<code>context.id</code> 或 <code>context.current_index</code> 发生变化。</li><li><strong>元数据延迟到达</strong>：在 <code>track.id</code> 保持稳定的情况下，<code>track.title</code>、<code>duration_ms</code> 等 metadata 字段从空值或占位值更新为正式值，应推送一次更新事件（此时 <code>revision</code> 应递增）。</li><li><strong>连接建立</strong>：任何新连接建立后，服务端必须立即推送一次完整的当前快照（见第 3.1 节）。</li></ul><h3 id="_5-2-不得推送新事件的情况" tabindex="-1">5.2 不得推送新事件的情况 <a class="header-anchor" href="#_5-2-不得推送新事件的情况" aria-label="Permalink to &quot;5.2 不得推送新事件的情况&quot;">​</a></h3><p>以下情况不得触发新的 <code>player_state</code> 事件推送：</p><ul><li><strong>进度的自然推进</strong>：播放过程中 <code>position_ms</code> 因时间流逝而增加。前端通过本地插值处理进度推进（见投影规范第 10.3 节），服务端不为此推送事件，也不递增 <code>revision</code>。</li><li><strong>内部状态变化但对外快照不变</strong>：服务端内部缓存刷新、日志记录、非播放相关配置变化等，不影响投影规范定义的任何快照字段时，不得推送事件。</li><li><strong>重复状态</strong>：若服务端内部产生了与上一次推送快照完全相同（<code>revision</code> 相同）的快照，不得再次推送。</li></ul><h3 id="_5-3-推送顺序保证" tabindex="-1">5.3 推送顺序保证 <a class="header-anchor" href="#_5-3-推送顺序保证" aria-label="Permalink to &quot;5.3 推送顺序保证&quot;">​</a></h3><p>服务端应确保在同一 SSE 连接上，事件的推送顺序与 <code>revision</code> 的递增顺序一致。若因内部并发导致顺序可能乱序，服务端在推送前必须先完成排序，而不是依赖前端对乱序做容错处理。</p><p>前端的 <code>revision</code> 去重逻辑（见第 6 节）是针对重连、慢速网络等传输层因素的防御机制，不是弥补服务端推送乱序的手段。</p><hr><h2 id="_6-revision-与去重" tabindex="-1">6. revision 与去重 <a class="header-anchor" href="#_6-revision-与去重" aria-label="Permalink to &quot;6. revision 与去重&quot;">​</a></h2><h3 id="_6-1-revision-的传输角色" tabindex="-1">6.1 revision 的传输角色 <a class="header-anchor" href="#_6-1-revision-的传输角色" aria-label="Permalink to &quot;6.1 revision 的传输角色&quot;">​</a></h3><p><code>revision</code> 在 SSE 传输协议中承担以下角色：</p><ul><li>通过 SSE <code>id</code> 字段传递，使浏览器重连时能携带最后已知的 revision。</li><li>作为前端去重的唯一依据，不依赖事件的到达顺序。</li></ul><h3 id="_6-2-前端去重规则" tabindex="-1">6.2 前端去重规则 <a class="header-anchor" href="#_6-2-前端去重规则" aria-label="Permalink to &quot;6.2 前端去重规则&quot;">​</a></h3><p>前端在收到每一条 <code>player_state</code> 事件后，必须执行以下判断：</p><ol><li>解析 <code>data</code> 字段，读取 <code>revision</code> 值。</li><li>将收到的 <code>revision</code> 与当前 UI 已渲染状态的 <code>revision</code> 进行比较。</li><li>若收到的 <code>revision</code> 严格小于当前已渲染的 <code>revision</code>，必须丢弃该事件，不得更新 UI。</li><li>若收到的 <code>revision</code> 等于当前已渲染的 <code>revision</code>，视为重复事件，应丢弃，不触发 UI 重渲染。</li><li>若收到的 <code>revision</code> 大于当前已渲染的 <code>revision</code>，应用该快照更新 UI。</li></ol><h3 id="_6-3-服务端不得依赖顺序假设" tabindex="-1">6.3 服务端不得依赖顺序假设 <a class="header-anchor" href="#_6-3-服务端不得依赖顺序假设" aria-label="Permalink to &quot;6.3 服务端不得依赖顺序假设&quot;">​</a></h3><p>服务端在设计推送逻辑时，不得假设&quot;事件一定按顺序到达前端&quot;。以下场景可能导致事件乱序或重复到达：</p><ul><li>网络拥塞导致事件延迟，与重连后的新事件交错到达。</li><li>客户端重连后收到初始快照，而此前发送的旧事件因网络缓冲延迟也在此时到达。</li><li>代理或中间件对 SSE 流进行缓冲后批量转发。</li></ul><p>服务端的职责是确保每条事件的 <code>revision</code> 正确，前端的职责是基于 <code>revision</code> 做去重，双方不得依赖传输层顺序保证。</p><hr><h2 id="_7-重连机制" tabindex="-1">7. 重连机制 <a class="header-anchor" href="#_7-重连机制" aria-label="Permalink to &quot;7. 重连机制&quot;">​</a></h2><h3 id="_7-1-浏览器自动重连行为" tabindex="-1">7.1 浏览器自动重连行为 <a class="header-anchor" href="#_7-1-浏览器自动重连行为" aria-label="Permalink to &quot;7.1 浏览器自动重连行为&quot;">​</a></h3><p>浏览器的 <code>EventSource</code> API 在连接断开后会自动发起重连，默认重连间隔约为 3 秒（具体行为由浏览器实现决定）。重连时，浏览器会在请求中携带 <code>Last-Event-ID</code> Header，其值为最后收到的 SSE <code>id</code> 字段值（即最后已知的 <code>revision</code>）。</p><p>服务端应接受携带 <code>Last-Event-ID</code> 的重连请求，并识别其中的值，但不得依赖 <code>Last-Event-ID</code> 实现增量补发逻辑（见第 7.2 节）。</p><h3 id="_7-2-重连后必须推送完整快照" tabindex="-1">7.2 重连后必须推送完整快照 <a class="header-anchor" href="#_7-2-重连后必须推送完整快照" aria-label="Permalink to &quot;7.2 重连后必须推送完整快照&quot;">​</a></h3><p>无论客户端重连时携带了何种 <code>Last-Event-ID</code>，服务端在重连连接建立后，必须立即推送当前设备的完整最新状态快照（full snapshot），而不是从客户端上报的 <code>revision</code> 之后开始补发历史事件。</p><p>理由：服务端不得假设客户端在断线期间的内部状态是完整且未被篡改的；完整快照是最安全、最简洁的重新同步方式。服务端不维护历史事件队列，也不做增量补发。</p><h3 id="_7-3-重连间隔控制" tabindex="-1">7.3 重连间隔控制 <a class="header-anchor" href="#_7-3-重连间隔控制" aria-label="Permalink to &quot;7.3 重连间隔控制&quot;">​</a></h3><p>服务端可以通过 SSE <code>retry</code> 字段建议客户端的重连间隔（单位：毫秒）。建议值为 <code>3000</code>（3 秒）至 <code>10000</code>（10 秒）之间：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>retry: 5000</span></span></code></pre></div><p>此字段应在连接建立时随初始快照事件一起发送，不需要在每条事件中重复。</p><h3 id="_7-4-重连期间的降级行为" tabindex="-1">7.4 重连期间的降级行为 <a class="header-anchor" href="#_7-4-重连期间的降级行为" aria-label="Permalink to &quot;7.4 重连期间的降级行为&quot;">​</a></h3><p>在 SSE 连接断开、重连尚未成功的期间，前端应启动对 <code>GET /api/v1/player/state</code> 的轮询作为降级兜底（见第 11 节）。一旦 SSE 重连成功并收到初始快照，前端必须立即停止轮询，切回 SSE 主通道。</p><hr><h2 id="_8-心跳机制" tabindex="-1">8. 心跳机制 <a class="header-anchor" href="#_8-心跳机制" aria-label="Permalink to &quot;8. 心跳机制&quot;">​</a></h2><h3 id="_8-1-心跳是必须实现的功能" tabindex="-1">8.1 心跳是必须实现的功能 <a class="header-anchor" href="#_8-1-心跳是必须实现的功能" aria-label="Permalink to &quot;8.1 心跳是必须实现的功能&quot;">​</a></h3><p>服务端必须定期向所有活跃的 SSE 连接发送心跳，以防止以下问题：</p><ul><li>客户端或中间代理因长时间无数据而错误地判断连接已断开，触发不必要的重连。</li><li>Nginx、负载均衡器等组件因空闲超时主动关闭 TCP 连接。</li></ul><h3 id="_8-2-心跳格式" tabindex="-1">8.2 心跳格式 <a class="header-anchor" href="#_8-2-心跳格式" aria-label="Permalink to &quot;8.2 心跳格式&quot;">​</a></h3><p>心跳必须使用 SSE 注释格式（见第 4.4 节），不得伪装为 <code>player_state</code> 事件：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>: heartbeat</span></span></code></pre></div><p>心跳注释行以冒号开头，符合 SSE 规范中注释行的定义，不会被浏览器 <code>EventSource</code> 的 <code>onmessage</code> 回调接收，不会触发任何事件处理逻辑。</p><h3 id="_8-3-心跳频率" tabindex="-1">8.3 心跳频率 <a class="header-anchor" href="#_8-3-心跳频率" aria-label="Permalink to &quot;8.3 心跳频率&quot;">​</a></h3><p>心跳发送间隔建议为 <strong>15 秒</strong>，不得超过 <strong>30 秒</strong>。具体间隔可由实现侧根据部署环境（如反向代理的空闲超时配置）调整，但必须有心跳机制，不允许不发送任何心跳。</p><p>若距上次推送 <code>player_state</code> 事件的时间已超过心跳间隔，服务端必须发送心跳，无论其间是否有播放状态变化。</p><hr><h2 id="_9-多设备场景" tabindex="-1">9. 多设备场景 <a class="header-anchor" href="#_9-多设备场景" aria-label="Permalink to &quot;9. 多设备场景&quot;">​</a></h2><h3 id="_9-1-一个连接只订阅一个设备" tabindex="-1">9.1 一个连接只订阅一个设备 <a class="header-anchor" href="#_9-1-一个连接只订阅一个设备" aria-label="Permalink to &quot;9.1 一个连接只订阅一个设备&quot;">​</a></h3><p>每个 SSE 连接必须绑定到单个 <code>device_id</code>，只推送该设备的状态事件。服务端不得在同一连接上混合推送多个设备的状态。</p><h3 id="_9-2-device-id-的过滤约束" tabindex="-1">9.2 device_id 的过滤约束 <a class="header-anchor" href="#_9-2-device-id-的过滤约束" aria-label="Permalink to &quot;9.2 device_id 的过滤约束&quot;">​</a></h3><p>服务端在推送事件时，必须严格过滤，确保每条推送的 <code>player_state</code> 事件中的 <code>device_id</code> 字段，与建立连接时请求参数中的 <code>device_id</code> 完全一致。如果内部状态更新来自其他设备，不得通过当前连接推送。</p><h3 id="_9-3-多设备监控" tabindex="-1">9.3 多设备监控 <a class="header-anchor" href="#_9-3-多设备监控" aria-label="Permalink to &quot;9.3 多设备监控&quot;">​</a></h3><p>若前端需要同时监控多个设备的播放状态，必须为每个设备建立独立的 SSE 连接，分别携带各自的 <code>device_id</code> 参数。服务端不提供多设备聚合推送能力，前端不得向单个连接传递多个 <code>device_id</code>。</p><hr><h2 id="_10-错误处理" tabindex="-1">10. 错误处理 <a class="header-anchor" href="#_10-错误处理" aria-label="Permalink to &quot;10. 错误处理&quot;">​</a></h2><h3 id="_10-1-参数错误-400" tabindex="-1">10.1 参数错误（400） <a class="header-anchor" href="#_10-1-参数错误-400" aria-label="Permalink to &quot;10.1 参数错误（400）&quot;">​</a></h3><p>若请求中缺少 <code>device_id</code> 或 <code>device_id</code> 为空字符串，服务端必须拒绝请求，返回 HTTP 400，不建立 SSE 连接。响应体必须符合 <code>api_v1_spec.md</code> 的统一 envelope 格式：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">40001</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;missing or invalid device_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;error_code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;E_INVALID_REQUEST&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;stage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;request&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;request_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;req_xxx&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_10-2-设备不存在-404" tabindex="-1">10.2 设备不存在（404） <a class="header-anchor" href="#_10-2-设备不存在-404" aria-label="Permalink to &quot;10.2 设备不存在（404）&quot;">​</a></h3><p>若 <code>device_id</code> 对应的设备在当前运行时中不存在，服务端必须返回 HTTP 404，不建立 SSE 连接。</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">40004</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;device not found&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;error_code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;E_DEVICE_NOT_FOUND&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;stage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;xiaomi&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;request_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;req_xxx&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_10-3-连接建立后的运行时错误" tabindex="-1">10.3 连接建立后的运行时错误 <a class="header-anchor" href="#_10-3-连接建立后的运行时错误" aria-label="Permalink to &quot;10.3 连接建立后的运行时错误&quot;">​</a></h3><p>若 SSE 连接已建立（HTTP 200 已发出），但服务端随后遇到需要关闭连接的运行时错误（例如鉴权过期、设备注销），服务端应在关闭连接前推送一条 <code>stream_error</code> 事件（格式见第 4.5 节），然后关闭连接。</p><p>客户端收到 <code>stream_error</code> 事件后，应根据 <code>error_code</code> 决定后续行为：</p><table tabindex="0"><thead><tr><th><code>error_code</code></th><th>建议行为</th></tr></thead><tbody><tr><td><code>E_AUTH_EXPIRED</code></td><td>触发认证刷新流程，刷新成功后重新建立 SSE 连接</td></tr><tr><td><code>E_DEVICE_NOT_FOUND</code></td><td>提示用户设备已离线，停止重连</td></tr><tr><td><code>E_SERVER_SHUTDOWN</code></td><td>等待服务恢复后重连，建议使用指数退避</td></tr></tbody></table><p>对于未在上表列出的 <code>error_code</code>，客户端应使用默认重连策略（指数退避，最大间隔不超过 60 秒）。</p><h3 id="_10-4-鉴权失败-401" tabindex="-1">10.4 鉴权失败（401） <a class="header-anchor" href="#_10-4-鉴权失败-401" aria-label="Permalink to &quot;10.4 鉴权失败（401）&quot;">​</a></h3><p>若项目启用了 API 鉴权，<code>GET /api/v1/player/stream</code> 请求在鉴权校验阶段失败时，必须返回 HTTP 401，不建立 SSE 连接。</p><p>鉴权校验机制与项目现有 Internal API 鉴权体系一致，具体由 <code>docs/spec/auth_runtime_recovery.md</code> 约束，本文档不重复定义鉴权细节。</p><h3 id="_10-5-服务不可用-503" tabindex="-1">10.5 服务不可用（503） <a class="header-anchor" href="#_10-5-服务不可用-503" aria-label="Permalink to &quot;10.5 服务不可用（503）&quot;">​</a></h3><p>若服务端当前因进程启动中、重载中或其他原因无法提供 SSE 服务，应返回 HTTP 503。客户端应在收到 503 后使用指数退避策略进行重连，初始等待时间不低于 5 秒。</p><hr><h2 id="_11-与-http-player-state-接口的关系" tabindex="-1">11. 与 HTTP player/state 接口的关系 <a class="header-anchor" href="#_11-与-http-player-state-接口的关系" aria-label="Permalink to &quot;11. 与 HTTP player/state 接口的关系&quot;">​</a></h2><h3 id="_11-1-定位分工" tabindex="-1">11.1 定位分工 <a class="header-anchor" href="#_11-1-定位分工" aria-label="Permalink to &quot;11.1 定位分工&quot;">​</a></h3><table tabindex="0"><thead><tr><th>接口</th><th>定位</th><th>适用场景</th></tr></thead><tbody><tr><td><code>GET /api/v1/player/stream</code></td><td><strong>主通道</strong></td><td>SSE 连接正常时的所有状态接收</td></tr><tr><td><code>GET /api/v1/player/state</code></td><td><strong>初始化 + 降级回退</strong></td><td>页面首次加载、SSE 断线重连期间的轮询兜底、调试核验</td></tr></tbody></table><h3 id="_11-2-两者基于同一快照构建器" tabindex="-1">11.2 两者基于同一快照构建器 <a class="header-anchor" href="#_11-2-两者基于同一快照构建器" aria-label="Permalink to &quot;11.2 两者基于同一快照构建器&quot;">​</a></h3><p><code>/player/stream</code> 推送的 <code>player_state</code> 事件数据，与 <code>/player/state</code> 响应中 <code>data</code> 的内容，必须由同一个状态快照构建器生成（见投影规范第 11 节）。两个接口不得维护各自独立的状态拼装逻辑。</p><p>同一时刻，若 <code>/player/state</code> 返回的快照与 <code>/player/stream</code> 最近推送的快照存在差异，以 <code>revision</code> 较大的为准。若两者 <code>revision</code> 相同但字段内容不同，应判定为快照构建器实现存在缺陷，需修复实现，而不是在文档层面兼容差异。</p><h3 id="_11-3-禁止并行竞争" tabindex="-1">11.3 禁止并行竞争 <a class="header-anchor" href="#_11-3-禁止并行竞争" aria-label="Permalink to &quot;11.3 禁止并行竞争&quot;">​</a></h3><p>在 SSE 连接正常运行期间，前端不得同时对 <code>/player/state</code> 发起轮询。两路状态来源并行运行，将导致不同 <code>revision</code> 的快照交替写入 UI，产生状态竞争与视觉闪烁。</p><p>以下行为明确禁止：</p><ul><li>SSE 连接正常时，仍以固定间隔轮询 <code>/player/state</code> 并将结果合并到 UI。</li><li>以&quot;SSE 可能不及时&quot;为由，在 SSE 之外保留一条并行的状态轮询链路。</li><li>前端将 SSE 事件与 HTTP 轮询结果合并后，自行裁决哪一份&quot;更准确&quot;。</li></ul><hr><h2 id="_12-前端消费约束" tabindex="-1">12. 前端消费约束 <a class="header-anchor" href="#_12-前端消费约束" aria-label="Permalink to &quot;12. 前端消费约束&quot;">​</a></h2><h3 id="_12-1-sse-为主通道-轮询为降级" tabindex="-1">12.1 SSE 为主通道，轮询为降级 <a class="header-anchor" href="#_12-1-sse-为主通道-轮询为降级" aria-label="Permalink to &quot;12.1 SSE 为主通道，轮询为降级&quot;">​</a></h3><p>前端必须优先建立 SSE 连接作为播放状态的主获取通道。页面加载时的初始化流程应为：</p><ol><li>通过 <code>GET /api/v1/player/state</code> 获取初始快照，立即渲染初始状态。</li><li>建立 <code>GET /api/v1/player/stream</code> 的 SSE 连接。</li><li>SSE 连接建立并收到初始 <code>player_state</code> 事件后，停止任何轮询，以 SSE 为唯一状态来源。</li></ol><p>若 SSE 连接建立失败（非 200 响应）或断开后重连失败，前端应切换为对 <code>/player/state</code> 的轮询模式，轮询间隔不低于 3 秒。一旦 SSE 重连成功并收到初始快照，必须立即停止轮询。</p><h3 id="_12-2-revision-去重必须执行" tabindex="-1">12.2 revision 去重必须执行 <a class="header-anchor" href="#_12-2-revision-去重必须执行" aria-label="Permalink to &quot;12.2 revision 去重必须执行&quot;">​</a></h3><p>前端在处理每一条 <code>player_state</code> 事件时，必须执行第 6.2 节定义的 <code>revision</code> 去重逻辑，不得因&quot;SSE 通常有序&quot;而跳过去重检查。</p><h3 id="_12-3-完整快照替换-禁止增量合并" tabindex="-1">12.3 完整快照替换，禁止增量合并 <a class="header-anchor" href="#_12-3-完整快照替换-禁止增量合并" aria-label="Permalink to &quot;12.3 完整快照替换，禁止增量合并&quot;">​</a></h3><p>每次收到 <code>player_state</code> 事件时，前端必须以事件中的完整快照替换本地状态，不得将事件 <code>data</code> 字段与已有状态做合并（merge）操作。增量合并逻辑可能导致旧字段残留，产生状态污染。</p><h3 id="_12-4-play-session-id-与-track-id-绑定行为" tabindex="-1">12.4 play_session_id 与 track.id 绑定行为 <a class="header-anchor" href="#_12-4-play-session-id-与-track-id-绑定行为" aria-label="Permalink to &quot;12.4 play_session_id 与 track.id 绑定行为&quot;">​</a></h3><p>前端在处理 SSE 事件时，关于 <code>play_session_id</code>、<code>track.id</code> 的消费规则与投影规范第 12.3 节完全一致，本文档不重复定义，应以投影规范为准。</p><h3 id="_12-5-心跳事件的处理" tabindex="-1">12.5 心跳事件的处理 <a class="header-anchor" href="#_12-5-心跳事件的处理" aria-label="Permalink to &quot;12.5 心跳事件的处理&quot;">​</a></h3><p>前端不得将心跳注释（<code>: heartbeat</code>）视为有效事件处理。浏览器 <code>EventSource</code> API 会自动忽略注释行，前端代码不需要为此添加额外处理逻辑。若使用自定义 SSE 解析器，必须确保其正确忽略以冒号开头的注释行。</p><h3 id="_12-6-stream-error-事件的处理" tabindex="-1">12.6 stream_error 事件的处理 <a class="header-anchor" href="#_12-6-stream-error-事件的处理" aria-label="Permalink to &quot;12.6 stream_error 事件的处理&quot;">​</a></h3><p>前端必须监听 <code>stream_error</code> 事件类型。收到后，不得无条件立即重连，应先读取 <code>error_code</code>，按第 10.3 节的建议行为处理。特别地，收到 <code>E_AUTH_EXPIRED</code> 时必须先完成认证刷新，再重新建立 SSE 连接；未完成认证刷新前，不得循环重连。</p><h3 id="_12-7-页面可见性变化" tabindex="-1">12.7 页面可见性变化 <a class="header-anchor" href="#_12-7-页面可见性变化" aria-label="Permalink to &quot;12.7 页面可见性变化&quot;">​</a></h3><p>当页面因切换到后台（<code>document.visibilityState == &quot;hidden&quot;</code>）导致 SSE 连接可能被浏览器暂停或断开时，前端应在页面重新变为可见（<code>visibilitychange</code> 事件触发且 <code>document.visibilityState == &quot;visible&quot;</code>）时检查 SSE 连接状态，并在连接已断开的情况下主动发起重连，而不是等待浏览器自动重连超时。</p><hr><h2 id="_13-与其他规范的关系" tabindex="-1">13. 与其他规范的关系 <a class="header-anchor" href="#_13-与其他规范的关系" aria-label="Permalink to &quot;13. 与其他规范的关系&quot;">​</a></h2><h3 id="_13-1-与投影规范的关系" tabindex="-1">13.1 与投影规范的关系 <a class="header-anchor" href="#_13-1-与投影规范的关系" aria-label="Permalink to &quot;13.1 与投影规范的关系&quot;">​</a></h3><p><code>docs/spec/player_state_projection_spec.md</code> 是本文档的上位规范。本文档定义的 SSE 传输协议，负责将投影规范所定义的状态快照通过 SSE 通道推送给消费方。本文档不定义快照的字段语义，所有字段语义以投影规范为准。</p><h3 id="_13-2-与-api-v1-契约的关系" tabindex="-1">13.2 与 API v1 契约的关系 <a class="header-anchor" href="#_13-2-与-api-v1-契约的关系" aria-label="Permalink to &quot;13.2 与 API v1 契约的关系&quot;">​</a></h3><p><code>docs/api/api_v1_spec.md</code> 是 v1 Public API 的唯一权威契约来源。<code>GET /api/v1/player/stream</code> 作为 v1 白名单接口，须遵守 <code>api_v1_spec.md</code> 的通用协议要求，包括命名空间约束、统一错误模型（适用于非 200 错误响应场景）与接口分级规则（Class C）。</p><h3 id="_13-3-与-webui-相关规范的关系" tabindex="-1">13.3 与 WebUI 相关规范的关系 <a class="header-anchor" href="#_13-3-与-webui-相关规范的关系" aria-label="Permalink to &quot;13.3 与 WebUI 相关规范的关系&quot;">​</a></h3><p><code>docs/spec/webui_playback_state_machine_spec.md</code> 定义 WebUI 如何消费播放状态并维护展示层状态机。WebUI 状态机的输入来源，在 SSE 可用时必须是本文档定义的 SSE 事件；在 SSE 不可用时才降级为 <code>/player/state</code> 轮询。WebUI 规范中凡涉及&quot;以哪个字段为权威依据&quot;的规则，以投影规范为准，不受传输通道变化影响。</p><h3 id="_13-4-与-playbackcoordinator-的关系" tabindex="-1">13.4 与 PlaybackCoordinator 的关系 <a class="header-anchor" href="#_13-4-与-playbackcoordinator-的关系" aria-label="Permalink to &quot;13.4 与 PlaybackCoordinator 的关系&quot;">​</a></h3><p>PlaybackCoordinator 的内部状态变化是服务端产生推送事件的来源。当 PlaybackCoordinator 产生导致 <code>revision</code> 递增的状态变化时，必须通知 SSE 推送组件，由其通过统一快照构建器生成快照并推送。PlaybackCoordinator 不直接写入 SSE 流；SSE 推送组件通过快照构建器与 PlaybackCoordinator 解耦。</p>`,181)]))}const _=a(i,[["render",s]]);export{u as __pageData,_ as default};
